# The toolbox build consumes only bin/ and versions.env; keep the context
# to exactly that. (.dockerignore is a symlink to this file: Docker reads
# only that name, Podman only this one.)
*
!bin/
!versions.env
